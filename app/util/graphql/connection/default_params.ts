import extendConnection from "./extend_connection";
import validateArgs from "./validate_args";

const defaultParams = {
  extendConnection,
  validateArgs,
};

export default defaultParams;
