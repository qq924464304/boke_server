# ===========================================
# 本地开发环境配置模板 (.env.example)
# 
# 使用方法：
#   cp .env.example .env.local
#   然后编辑 .env.local 填入真实值
#
# ⚠️ 注意：.env.local 必须加入 .gitignore，禁止提交！
# ===========================================

# ========== 应用基础配置 ==========
NODE_ENV=development
APP_ENV_CONFIG_TYPE_NAME=dev

# ========== 数据库 (Prisma) ==========
# 格式: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/ysys_dev?schema=public"

# ========== Redis 配置 ==========
APP_IS_USE_REDIS=FALSE
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here
REDIS_DB=0

# ========== 服务配置 ==========
PORT=4000
HOST=0.0.0.0

# ========== 其他可选配置 ==========
# JWT_SECRET=your_jwt_secret
# API_PREFIX=/api/v1